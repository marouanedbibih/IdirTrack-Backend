server:
  port: 8080

spring:
  application:
    name: backend
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://db-instance-idirtrack.cd0ikwgi62ui.eu-west-3.rds.amazonaws.com:3306/idirtrack?createDatabaseIfNotExist=true&useSSL=false
    username: admin
    password: admin1234
  jpa:
    hibernate:
      ddl-auto: update
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect
traccar:
  api:
    url: http://152.228.219.146:8082

logging:
  level:
    org.springframework: DEBUG
    hibernate: DEBUG
    security: DEBUG
    com.idirtrack: DEBUG